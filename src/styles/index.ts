import { StyleSheet } from 'react-native';
import * as Colors from './colors';

const Styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: Colors.default.white.basic,
  },
});

export { Styles, Colors };
